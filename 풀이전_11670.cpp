#include <stdio.h>
#include <set>

using namespace std;

typedef struct {
	int first;
	int second;
	char p;
}node[2500];
int main() {
	int n;
	scanf("%d", &n);

	int tempN = n;
	bool done = true;



	for( int i = 0 ; i <n ; i++){
		int a, b;
		scanf("%d", &a, &b);
	}

	if( done ) printf("")
	return 0;
}